# Required: your OpenAI API key. The account/project must have access to
# gpt-image-1 (this model needs org verification in the OpenAI dashboard
# under Settings -> Organization -> Verifications) and gpt-4o (for the
# vision analyst / critic agents).
OPENAI_API_KEY=sk-...
